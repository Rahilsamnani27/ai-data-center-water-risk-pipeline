version: 2

models:
  - name: mart_water_stress
    columns:
      - name: site_name
        tests:
          - not_null
          - unique
      - name: stress_score
        tests:
          - dbt_utils.accepted_range:
              min_value: 0
              max_value: 1